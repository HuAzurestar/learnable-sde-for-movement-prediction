# RC-NEX380-20260827-001 failure record

Status: closed with verified workarounds; no failed run is represented as a
successful data product. Scope: NEX-380 OSM distance-field data pipeline.

## F1 — system osmium/GDAL commands absent

- Probe: `$commands='osmium','gdal_rasterize','gdal_proximity.py'; foreach($name in $commands){ if(Get-Command $name -ErrorAction SilentlyContinue){"FOUND $name"}else{"MISSING $name"} }`
- Original/reproduced output: `MISSING osmium`, `MISSING gdal_rasterize`,
  `MISSING gdal_proximity.py`; PowerShell probe exit status `0`; measured wall
  time `0.212 s` on 2026-08-27.
- Impact/no-output statement: the planned CLI path could not start and wrote no
  GeoTIFF or manifest.
- Resolution: use the declared Python stack (`osmium 4.3.1`, pyproj 3.7.1,
  rasterio 1.4.4, SciPy 1.15.3, Shapely 2.1.2); install repository-local,
  ignored osmium-tool 1.19.1 only for PBF extraction.
- Verification: Zhejiang and Westminster builds completed; all output hashes
  are recorded in `docs/reports/NEX-380.md`.

## F2 — full complete_ways smoke exceeded the gate

- Command: `.venv-osm\Scripts\python.exe scripts\build_osm_distance_fields.py --pbf artifacts\nex380\gb_london_extract.osm.pbf --output-dir artifacts\nex380\gb_london_smoke --region gb_london_smoke --bounds -0.15 51.49 -0.10 51.53 --crs EPSG:27700 --resolution-m 50 --max-distance-m 2000 --location-index flex_mem --assume-bounds-covered --coverage-note "Verified interior Westminster smoke window." --source-url https://download.geofabrik.de/europe/united-kingdom-latest.osm.pbf`
- Evidence/status: Python geometry scanning of the 154,655,509-byte
  `complete_ways` extract was terminated when the foreground smoke gate reached
  `600 s`; status `terminated_by_smoke_gate` (no successful process exit code).
- Impact/no-output statement: the target contained no manifest and made no
  coverage claim.
- Workaround: build equal-bbox `simple` and `complete_ways` 34 MB extracts,
  then require exact comparison of the four resulting GeoTIFF files.
- Verification: `scripts/compare_osm_distance_fields.py` returned `0`; all four
  files have identical SHA-256 and zero differing pixels. Evidence is
  `artifacts/nex380/gb_westminster_simple_vs_complete.json` and summarized in
  `docs/reports/NEX-380.md`.

## F3 — adapter import failed before repository-root bootstrap

- Command: `.venv-osm\Scripts\python.exe scripts\augment_cond_slices_osm.py --input-root E:\Worktable\NEX-317_data_sources\cond_slices\val --output-root artifacts\nex380\cond_slice_smoke --manifest artifacts\nex380\gb_westminster_smoke\manifest.json`
- Original error/status: `ModuleNotFoundError: No module named 'data.osm'`;
  script exit status `1`; observed failing command bundle wall time `19.2 s`.
- Impact/no-output statement: import failed before reading or writing a parquet;
  no augmentation manifest was published.
- Fix: prepend the repository root when the script is invoked directly.
- Verification: the same direct-script entry point completed the real 4,821-row
  validation slice and is covered by pytest.

## F4 — masked uint8 coverage conversion failed

- Command: same adapter command as F3 after the import fix.
- Original error/status: `TypeError: Cannot convert fill_value nan to dtype
  uint8`; caught script exit status `2`; measured command wall time `10.4 s`.
- Impact/no-output statement: sampling stopped before parquet/manifest
  publication; no output was claimed.
- Fix: convert the masked coverage band to float before filling NaN, then derive
  `has_osm` explicitly.
- Verification: the real slice contains 2,345 covered and 2,476 uncovered rows;
  tests cover missing coverage and atomic publication.
