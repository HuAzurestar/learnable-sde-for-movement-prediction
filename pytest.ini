; pytest 配置。
; addopts 禁用 asyncio 插件：本机全局安装的 pytest_asyncio 缺 backports.asyncio 依赖会崩溃，
; 且本项目无 asyncio 测试，禁用无害（详见 CONTRIBUTING.md「运行测试」）。
; importlib 模式避免 pytest 因仓库根 __init__.py 把父目录置前，误导入兄弟目录中的
; 同名 learnable_sde 包。
[pytest]
testpaths = tests
addopts = -p no:asyncio --import-mode=importlib
